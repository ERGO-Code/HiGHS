name: build-unix

on: ["push"]

jobs:
  debug:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v2

    - name: make
      run: julia --project=. make.jl
           
